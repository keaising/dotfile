#!/bin/zsh

# setpx
set -e
export TPM_REPO='~/code/github.com/tmux-plugins/tpm'
if [ ! -d "$TPM_REPO" ]; then
	git clone https://github.com/tmux-plugins/tpm.git $TPM_REPO
fi

cd $TPM_REPO
git pull origin master
